import Elasticsearch = require('elasticsearch');
import { Microfleet } from '../';
/**
 * Relative priority inside the same plugin group type
 */
export declare const priority = 0;
export declare const name = "elasticsearch";
export declare const type: "database";
export declare function attach(this: Microfleet, params?: any): {
    /**
     * @returns Elasticsearch connection.
     */
    connect(): Promise<Elasticsearch.Client>;
    /**
     * @returns Closes elasticsearch connection.
     */
    close(): Promise<void>;
};
