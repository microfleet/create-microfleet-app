import { HttpStatusError as HttpCoreError } from 'common-errors';
export declare class HttpStatusError extends HttpCoreError {
    errors?: Error[];
    field?: string;
    status?: number;
    status_code?: number;
    /**
     * @param statusCode
     * @param message
     * @param field
     */
    constructor(statusCode: number, message?: string, field?: string);
    /**
     * Adds error to the http status
     */
    addError(error: Error): this;
}
